apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: picoclaw-data
  namespace: picoclaw
  labels:
    app.kubernetes.io/name: picoclaw
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
