apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-menghi.uy
spec:
  dnsNames:
    - "*.menghi.uy"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  secretName: wildcard-menghi.uy-tls-cert
