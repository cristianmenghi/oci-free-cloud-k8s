apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: elastic-credentials
  namespace: elastic-system
spec:
  refreshInterval: 3m
  secretStoreRef:
    kind: ClusterSecretStore
    name: oracle-vault
  target:
    name: elastic-credentials
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      engineVersion: v2
      data:
        elastic: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: elastic-password-v1
        version: CURRENT
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: elastic-es-secure-settings
  namespace: elastic-system
spec:
  refreshInterval: 3m
  secretStoreRef:
    kind: ClusterSecretStore
    name: oracle-vault
  target:
    name: elastic-es-secure-settings
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      engineVersion: v2
      data:
        xpack.security.encryptionKey: "{{ .key }}"
  data:
    - secretKey: key
      remoteRef:
        key: elastic-encryption-key-v1
        version: CURRENT
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kibana-secure-settings
  namespace: elastic-system
spec:
  refreshInterval: 3m
  secretStoreRef:
    kind: ClusterSecretStore
    name: oracle-vault
  target:
    name: kibana-secure-settings
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      engineVersion: v2
      data:
        xpack.encryptedSavedObjects.encryptionKey: "{{ .key }}"
  data:
    - secretKey: key
      remoteRef:
        key: kibana-encryption-key-v1
        version: CURRENT
